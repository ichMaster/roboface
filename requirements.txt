# RoboFace server -- runtime dependencies.
# Each entry is pulled in by the phase that first needs it; nothing is added ahead of
# the roadmap. v0.1 needs the websocket server and the .env reader, nothing more.
fastapi>=0.115
uvicorn[standard]>=0.32
websockets>=13.0
python-dotenv>=1.0
# Chat and vision, and nothing else (MISSION: Gemini only). Imported lazily inside
# providers/gemini.py so the default test suite runs without it.
google-genai>=1.0
# Speech, from v1.1. The ElevenLabs adapter streams over plain HTTP rather than through a vendor
# SDK -- the endpoint is one POST and the response is a byte stream, so an SDK would add a
# dependency without removing any code. Imported lazily inside the adapter.
httpx>=0.27
